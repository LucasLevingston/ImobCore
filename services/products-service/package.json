{
  "name": "products-service",
  "version": "0.1.0",
  "private": true,
  "description": "Microservice de produtos (CRUD, busca, paginação, filtros) — Fastify + Prisma + PostgreSQL",
  "scripts": {
    "dev": "echo \"products-service: implementação pendente — Fase 4\" && exit 0"
  }
}
