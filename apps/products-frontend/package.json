{
  "name": "products-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Micro Frontend de produtos (listagem, cadastro, edição, exclusão, busca, paginação, detalhes) — Next.js App Router",
  "scripts": {
    "dev": "echo \"products-frontend: implementação pendente — Fase 5\" && exit 0"
  }
}
