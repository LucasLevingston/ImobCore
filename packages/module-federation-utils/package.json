{
  "name": "@microfrontends/module-federation-utils",
  "version": "0.1.0",
  "private": true,
  "description": "Helpers de build pra config do Module Federation (ModuleFederationPlugin) — evita duplicar/desatualizar a lista de shared dependencies entre auth-frontend e properties-frontend",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  }
}
