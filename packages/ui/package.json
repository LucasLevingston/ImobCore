{
  "name": "@microfrontends/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Design system compartilhado (shadcn/ui) — Button, Input, Card, Modal, Toast, Loading, Error, Layout, Header, Sidebar",
  "scripts": {
    "build": "echo \"packages/ui: implementação pendente — Fase 1\" && exit 0"
  }
}
